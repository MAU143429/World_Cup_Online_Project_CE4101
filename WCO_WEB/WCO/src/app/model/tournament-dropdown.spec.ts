import { TournamentDropdown } from './tournament-dropdown';

describe('TournamentDropdown', () => {
  it('should create an instance', () => {
    expect(new TournamentDropdown()).toBeTruthy();
  });
});
